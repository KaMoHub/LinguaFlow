{% extends 'base.html' %}

{% block title %}Удаление карточки - {{ card.word_original }}{% endblock %}

{% block content %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h4>🗑️ Удаление карточки</h4>
            </div>
            <div class="card-body">
                <div class="alert alert-warning">
                    <h5>Вы уверены, что хотите удалить эту карточку?</h5>
                </div>

                <div class="card mb-3">
                    <div class="card-body">
                        <strong>Слово:</strong> {{ card.word_original }}<br>
                        <strong>Перевод:</strong> {{ card.word_translation }}<br>
                        <strong>Колода:</strong> {{ card.deck.title }}<br>
                        <small class="text-muted">Создано: {{ card.created_at|date:"d.m.Y H:i" }}</small>
                    </div>
                </div>

                <p class="text-danger">
                    <strong>Внимание!</strong> Это действие нельзя будет отменить.
                    Будут удалены все связанные аудио файлы.
                </p>

                <form method="post">
                    {% csrf_token %}
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-danger">Да, удалить</button>
                        <a href="{% url 'card_detail' card.id %}" class="btn btn-secondary">Отмена</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}